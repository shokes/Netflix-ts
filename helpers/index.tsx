// formats the date to by taking just the year
export const formatDate = (year: string) => {
  return year.slice(0, 4);
};

// for the play button
export const handlePlay = () => {
  alert('please use the real Netflix lol');
};
