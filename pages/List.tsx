import { Navigation } from '../components';

const List = () => {
  // const { heroMovie } = useSelector((store: RootState) => store.shows);
  return (
    <section>
      <Navigation />
      <div>List</div>;
    </section>
  );
};

export default List;
